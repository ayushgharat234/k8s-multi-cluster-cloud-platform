apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: payment-pipeline
description: "storefront-payment → GKE Spoke — standard deployment"
serialPipeline:
  stages:
  - targetId: gke-spoke
    profiles: [gke-payment]
    strategy:
      standard:
        verify: false
