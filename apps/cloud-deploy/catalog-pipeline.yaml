apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: catalog-pipeline
description: "storefront-catalog → EKS Spoke — standard deployment"
serialPipeline:
  stages:
  - targetId: eks-spoke
    profiles: [eks]
    strategy:
      standard:
        verify: false
