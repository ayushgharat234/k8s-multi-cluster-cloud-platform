apiVersion: v1
kind: LimitRange
metadata:
  name: nexus-app-limits
  namespace: nexus-app
spec:
  limits:
  - type: Container
    default:
      cpu: 200m
      memory: 256Mi
    defaultRequest:
      cpu: 50m
      memory: 64Mi
    max:
      cpu: "2"
      memory: 1Gi
