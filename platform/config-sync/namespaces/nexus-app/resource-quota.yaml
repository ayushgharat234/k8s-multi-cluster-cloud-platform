apiVersion: v1
kind: ResourceQuota
metadata:
  name: nexus-app-quota
  namespace: nexus-app
spec:
  hard:
    pods: "20"
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    services.loadbalancers: "2"
    secrets: "20"
