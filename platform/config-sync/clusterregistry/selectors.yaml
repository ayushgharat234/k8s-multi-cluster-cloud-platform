apiVersion: configmanagement.gke.io/v1
kind: ClusterSelector
metadata:
  name: selector-gcp
spec:
  selector:
    matchLabels:
      provider: gcp
---
apiVersion: configmanagement.gke.io/v1
kind: ClusterSelector
metadata:
  name: selector-aws
spec:
  selector:
    matchLabels:
      provider: aws
